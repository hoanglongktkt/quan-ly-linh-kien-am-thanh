<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /
  # Không rewrite API — để Node/Passenger hoặc reverse proxy xử lý
  RewriteCond %{REQUEST_URI} !^/api/ [NC]
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteRule ^ index.html [L]
</IfModule>
